#pragma once

namespace Pulsarion::Shader
{
    struct TomlConfig
    {
        // TODO: Implement, this should be a struct that holds project configuration, and not per shader configuration
    };
}
