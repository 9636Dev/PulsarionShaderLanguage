#include "Optimizer.hpp"

namespace Pulsarion::Shader
{
    void DefaultOptimizer::Optimize(SyntaxNode& ast)
    {
        // We will need to traverse the AST and optimize it
        // This is a placeholder for now
    }
}
